## 1. 领域模型与回测任务骨架

- [x] 1.1 定义 BacktestJob、BacktestResult、BacktestConfig 等领域对象与状态流转规则
- [x] 1.2 在 `application` 层新增回测任务编排服务（创建任务、执行任务、查询任务）
- [x] 1.3 在 `infrastructure` 层新增回测任务与结果仓储接口及基础实现

## 2. 时点一致特征重建与信号阶段

- [x] 2.1 实现历史时点特征构建器，按调仓日读取可见财务/行情数据并输出评分输入
- [x] 2.2 接入现有投资质量评分器，生成每期分数、分组与规则信号
- [x] 2.3 增加样本剔除与原因记录（字段缺失、不可交易、口径不满足）

## 3. 组合构建、调仓执行与成本记账

- [x] 3.1 实现 Top-N / Top-Quantile 等权组合构建规则
- [x] 3.2 实现调仓执行与持仓演进（目标权重、持仓变更、组合收益计算）
- [x] 3.3 实现交易成本与换手扣减逻辑，支持参数化配置

## 4. 指标评估与结果持久化

- [x] 4.1 实现绩效指标计算（年化、夏普、最大回撤、超额、换手）
- [x] 4.2 实现诊断指标计算（IC/RankIC、分层收益、单调性）
- [x] 4.3 持久化结果摘要与曲线明细，并提供查询模型

## 5. 接口与验证

- [x] 5.1 新增 CLI/API 入口：提交回测任务、查询状态、获取结果摘要
- [x] 5.2 补充单元测试与集成测试（时点一致性、成本记账、指标计算）
- [x] 5.3 使用固定历史区间进行验收回测并沉淀基准样例
