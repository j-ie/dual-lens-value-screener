"""公司 AI 分析结构化输出中分数字段的领域语义（与大模型提示约定一致）。

本模块仅承载概念说明，供应用层提示词与 API 文档对齐；不参与运行时计算。

- **一致性分**（`ai_score`）：在已给 JSON 范围内，信息完整度与「规则筛分 / DCF / 叙述」是否自洽；
  信号冲突会降低该分，**不**等同于标的优劣或是否便宜。
- **机会倾向分**（`opportunity_score`）：在已给数据下，对安全边际是否充裕、是否接近「黄金坑」式机会的
  **主观倾向**；须结合估值与质量信号综合判断，且 **dcf.ok 为 true 时**：对银行/保险等金融业应认知
  DCF 为粗代理，**不得**仅因简化 DCF 低于市价就将机会分打到极端低位，除非 JSON 中其他字段
  共同支持「显著高估」叙事。二者均非投资建议。
"""

__all__: list[str] = []
